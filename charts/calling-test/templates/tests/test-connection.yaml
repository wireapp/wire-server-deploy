apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "calling-test.fullname" . }}-test-connection"
  labels:
    {{- include "calling-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "calling-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
