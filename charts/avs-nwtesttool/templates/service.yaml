apiVersion: v1
kind: Service
metadata:
  name: {{ include "avs-nwtesttool.fullname" . }}
  labels:
    {{- include "avs-nwtesttool.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "avs-nwtesttool.selectorLabels" . | nindent 4 }}
