apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "avs-nwtesttool.fullname" . }}-test-connection"
  labels:
    {{- include "avs-nwtesttool.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "avs-nwtesttool.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
