# PostgreSQL Client Authentication Configuration File
# {{ ansible_managed }}

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             all                                     peer

# IPv4 local connections:
host    all             all             127.0.0.1/32            md5

# IPv6 local connections:
host    all             all             ::1/128                 md5

# Allow replication connections from replica nodes on primary node
# but we are allowing all connections for all users from 192.168.122.0/24

# Allow connections from application servers (adjust as needed)
# host    all             all             10.0.0.0/8              md5
host    all             all             192.168.122.0/24          md5
