implement PostgreSQL high availability cluster with repmgr

Adds 3-node PostgreSQL cluster with automated failover using repmgr to ensure Wire service continuity during database failures. Includes comprehensive fencing mechanisms and safety checks.