EXAMPLE: Control plane only K8s nodes
=====================================

This example deploys a group of machines ready to deploy a Kubernetes cluster on top, that
only consists of control plane machines.


### Characteristics:

* deployed on Hetzner
* no load-balancing
